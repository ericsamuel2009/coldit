const { Router } = require('express');
const router = Router();
const _ = require('underscore');
const db = require('./../../db/database.js');

    
    //RUTAS
router.get('/', (req,res)=>{ //TODO LOS PORDUCTOS
    res.send("registro no encontrado")

  })